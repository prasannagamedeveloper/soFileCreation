apply plugin: 'cpp'

model {
    components {
        main(NativeLibrarySpec) {
            targetPlatform 'android'
            sources {
                cpp {
                    source {
                        srcDir 'src'
                        include '**/*.cpp'
                    }
                }
            }
        }
    }
}
